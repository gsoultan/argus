# Secrets for argus-agent, read by systemd. Installed 0600 root:argus.
#
# Every value the config file references with ${VAR} must appear here, or the
# service refuses to start rather than running with an empty credential.
# The agent takes its control-plane settings as flags in the unit file; this
# exists so an operator has one consistent place to put overrides.
